# recipes/

One markdown file per recipe, sorted into `breakfast/`, `lunch/`, and `dinner/`. The app parses these files directly — follow [../docs/recipe-format.md](../docs/recipe-format.md), add new files to [manifest.json](manifest.json), and run `python3 tools/validate.py` from the repo root after any change.
